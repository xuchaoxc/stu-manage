
export class ListAllClazsDto {
  readonly id: string;
  readonly classNo: string;
  readonly className: string;
  readonly classDate: Date;
/*   readonly limit: number;
  readonly offset: number; */

/*   constructor(clazs: Clazs) {
    this.id = clazs.id;
    this.classNo = clazs.class_no;
    this.className = clazs.class_no;
    this.classDate = clazs.class_date; 
  }*/ 
}